<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="container">
        <div class="content">
            <% if (companys.length > 0) { %> <% companys.forEach(company => { %>
                <h3 class="title"><%= company.companyName %> <%= company.serviceType %></h3>
                <p><%= company.location %></p>
                <% }) %> <% } else { %>
                <p>Não há companias para exibir</p>
                <% } %>      
        </div>
    </div>
</body>
</html>